<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_search_network"
        android:title="Buscar IPs"
        app:showAsAction="ifRoom"
        android:icon="@android:drawable/ic_menu_search" />
    <item
        android:id="@+id/action_export_report"
        android:title="Exportar Relatório (PDF)"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_backup"
        android:title="Backup (JSON)"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_import"
        android:title="Importar (JSON)"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_logs"
        android:title="Histórico"
        app:showAsAction="ifRoom"
        android:icon="@android:drawable/ic_menu_recent_history" />
</menu>
